name: Test And Build

env:
  GRADLE_OPTS: '-Dorg.gradle.daemon=false -Dkotlin.compiler.execution.strategy=in-process'
  JAVA_VERSION: 11
  SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
  SIGNING_KEY_ID: ${{ secrets.SIGNING_KEY_ID }}
  SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
  REPO_CREDENTIAL_USER: ${{ secrets.REPO_CREDENTIAL_USER }}
  REPO_CREDENTIAL_PASSWORD: ${{ secrets.REPO_CREDENTIAL_PASSWORD }}
  LIB_VERSION: ${{ github.ref_name }}
on:
  pull_request:
  release:
    types: [published]
permissions:
  contents: read
jobs:

  build:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'adopt'
    - name: Lint test
      uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
      with:
        arguments: lint test
    - name: Run Unit Tests
      uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
      with:
        arguments: test
    - name: Build with Gradle
      uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
      with:
        arguments: build

  sync:
    name: Publish to public repo
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: print folder
        run: ls ./
      - name: create deploy folder
        run: mkdir deploy
      - name: copy folders
        run: cp -R ./app ./gradle .gitignore LICENSE build.gradle gradle.properties gradlew gradlew.bat settings.gradle ./deploy
      - name: replace texts
        run: sed -i "s/include ':mylibrary'//g" ./deploy/settings.gradle
      - name: Pushes to another repository
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.REPO_CREDENTIAL_PASSWORD }}
        with:
          source-directory: 'deploy'
          destination-github-username: 'mmachado53'
          destination-repository-name: 'android-sample-lib-public'
          user-email: mmachado53@gmail.com
          target-branch: main

  publish:
    name: Publish
    needs: [ build ]
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'adopt'
      - name: Build with Gradle
        uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
        with:
          arguments: build
      - name: Publish to GitHub packages repository
        uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
        with:
          arguments: publishReleasePublicationToGitHubPackagesRepository